🧹 Implement Resend email sending in reports

🎯 What:
Implemented the `TODO` for sending reports via email using the `Resend` API in `app/actions/reports.ts`. Cleaned up placeholder debug logs and added safe fallbacks for missing email properties.

💡 Why:
This replaces a commented-out block of code with a fully functional feature while keeping the file clean and readable.

✅ Verification:
- Added `resend` client setup in `lib/resend.ts`.
- Ensured type safety and robust null checking (`if (!user.email)`).
- Validated via `npm run lint`, `npm test`, and `npm run build` which compiled successfully without typing issues.

✨ Result:
Report emails can now be successfully delivered utilizing the Resend Node.js SDK, fulfilling the application's requirement for automated report generation and distribution.
